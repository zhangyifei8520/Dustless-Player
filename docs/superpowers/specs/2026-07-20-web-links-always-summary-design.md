# 图文链接统一摘要卡设计

## 目标

所有非视频收藏在首页显示器中直接渲染可爱的本地摘要卡，不读取或展示第三方网页正文。

## 行为

- Bilibili 与 YouTube 保持现有嵌入播放器。
- 其他链接展示现有 `sim-reader-summary` 卡片：标题、收藏摘要、缩略图和“打开原链接”。
- 缩略图继续优先使用收藏库项目的 `thumbnail` 字段。
- 不从播放器调用 `/api/reader`，因此不会出现读取中的纯蓝网页区域。

## 验收

- 图文卡片初始渲染即为摘要卡。
- 图文路径不输出 iframe、读取状态或网页正文。
- 视频路径不变。
