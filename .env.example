# Copy to .env and fill in to enable AI classification for newly added cartridges
# (/api/classify). Without it the library page falls back to local keyword rules.
ANTHROPIC_API_KEY=
# Optional: override the model used for classification (default: claude-haiku-4-5)
# CLASSIFIER_MODEL=claude-haiku-4-5
